import Max from "../../assets/max_logo.svg";

const Logo = () => {
  return (
    <div>
      <img
        src={Max}
        alt="Logo"
        style={{
          width: "50px",
        }}
      />
    </div>
  );
};

export default Logo;
